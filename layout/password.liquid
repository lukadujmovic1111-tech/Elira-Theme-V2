<!doctype html>
<html lang="{{ request.locale.iso_code }}">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ shop.name }}</title>
  {%- if page_description -%}
    <meta name="description" content="{{ page_description | escape }}">
  {%- endif -%}
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Archivo:wght@400;500;600;700&family=Cormorant+Garamond:ital,wght@1,500;1,600&display=swap" rel="stylesheet">
  {% style %}
    :root {
      --c-ink: {{ settings.color_ink | default: '#1B1A18' }};
      --c-body: {{ settings.color_body | default: '#4A463F' }};
      --c-accent: {{ settings.color_accent | default: '#5E6B53' }};
      --c-bone: {{ settings.color_bone | default: '#F5F2EC' }};
      --c-sale: {{ settings.color_sale | default: '#A8472F' }};
      --c-accent-soft: {{ settings.color_accent | default: '#5E6B53' | color_mix: '#FFFFFF', 14 }};
      --c-ink-soft: {{ settings.color_ink | default: '#1B1A18' | color_modify: 'alpha', 0.55 }};
    }
  {% endstyle %}
  {{ 'base.css' | asset_url | stylesheet_tag }}
  {{ content_for_header }}
</head>
<body class="template-password">
  <main id="MainContent" role="main">
    {{ content_for_layout }}
  </main>
</body>
</html>
